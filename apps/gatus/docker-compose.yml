services:
  gatus:
    image: ghcr.io/twin/gatus:v5.10.0
    env_file:
      ./config/config.sops.env
    ports:
      - '8080:80'
    volumes:
      - ./config/config.yaml:/config/config.yaml:ro
    restart: unless-stopped
